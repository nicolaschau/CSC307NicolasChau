{
  "name": "csc-307-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run --workspace=express-backend dev & npm run --workspace=react-frontend dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "mongodb": "^6.20.0",
    "mongoose": "^8.19.1"
  }
}
